<?php
/**
 * @var Stores $block
 */

use \Ls\Omni\Block\Stores\Stores;
$hours = $block->getData('hours');
?>
<?php if (!empty($hours)) :
    ?>
    <div class="store-opening-hours">
        <label><i class="fas fa-business-time"></i><strong><?= __('Hours'); ?></strong></label>
        <table class="table table-borderless">
            <?php
            foreach ($hours as $h) :
                ?>
                <tr>
                    <?= $block->getFormattedHours($h); ?>
                </tr>
            <?php
            endforeach;
            ?>
        </table>
    </div>
<?php endif; ?>
